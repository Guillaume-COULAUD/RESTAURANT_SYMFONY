{% extends 'base.html.twig' %}

{% block title %}Detail allergen{% endblock %}

{% block body %}

<table class="table">
        <thead>
            <tr>
                <th scope="col">Nom </th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>
                      {{allergen.name}}
                </td>
                <td>  <a href="{{ path('modify_allergen', {'id': allergen.id}) }}"><button type="button" class="btn btn-primary">Modifier</button></a> </td>
                <td><a href="{{ path('delete_allergen', {'id': allergen.id}) }}"><button type="button" class="btn btn-danger">Supprimer</button></a>                </td>
            </tr>
        </tbody>

        <tr>
            <td>
                <a href="{{ path('list_allergen') }}"> Revenir aux allergenes </a>
            </td>
        </tr>
    </table>
               
{% endblock %}