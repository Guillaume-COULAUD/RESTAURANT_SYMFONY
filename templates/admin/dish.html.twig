{% extends 'base.html.twig' %}

{% block title %}Liste des plats{% endblock %}

{% block body %}

<style>
.example-wrapper{
    width: 100vw;
    background-color: #C0C0C0;
    display: flex;
    flex-direction: column;
    align-items: center;
    border: 1px solid;
}

.tableau{
    width: 100%;
    display: flex;
    justify-content: space-around;
    margin-top: 2vh;
}

tr{
    width: 100vh;
    display: flex;
    justify-content: space-around;
    margin-top: 1vh;
}
td{
    width: 25%;
}

.image{
    height: 5vh;
}

.image_plat{
    height: 5vh;
}
</style>

{% for message in app.flashes('notice') %}
        <div class="flash-notice">
            {{ message }}
        </div>
{% endfor %}

<h1>Lise des plats</h1>
<p><a href="{{ path('new_dish') }}">Ajouter un plat </a></p>
 <table class="tableau">
        <tr clas="ligne_tab">
            <th>Image</th>
            <th>Nom</th>
            <th>Calories</th>
            <th>Prix</th>
        </tr>
        
{% for dish in dishes %}
   
        <tr class="ligne_tab">
            <td class="image">
                <img class="image_plat" src="{{dish.Image}}"/>
            </td>
            <td>{{ dish.name }}</td>
            <td>{{ dish.calories }}</td>
            <td>{{ dish.price }}</td>
            <td><a href="{{ path('dish_detail', {'id': dish.id}) }}">Voir plus de détails</a></td>
        </tr>
    
{% endfor %}
        
    </table>
{% endblock %}