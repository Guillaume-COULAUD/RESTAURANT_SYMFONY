{% extends 'base.html.twig' %}

{% block title %}afficher Equipe{% endblock %}

{% block body %}



  <table class="table">
        <thead>
            <tr>
                <th scope="col">Nom</th>
                <th scope="col">Prénom</th>
                <th scope="col">Email</th>
                <th scope="col">Job</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>   {{user.lastname}}</td>
                <td>  {{user.firstName}}</td>
                <td>  {{user.Email}}</td>
                <td>  {{user.JobTitle}} </td>
            
            </tr>
        {% endfor %}
    </tbody>
    </table>

{% endblock %}
